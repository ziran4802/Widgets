# Widgets 开发约束

- 每次改动完成后，都必须创建一个对应的 Git commit，以便后续追踪和回滚。

## 开发入口

- 先阅读 [`README.md`](README.md) 了解项目范围，再按需要阅读 [`docs/development.md`](docs/development.md) 和 [`docs/status.md`](docs/status.md)。
- 公开文档只描述当前代码和可复现证据；历史记录与人工尚未完成的验证必须明确区分。

## 项目范围

- 这是 Windows 桌面小组件的 vibe coding 练手项目，个人使用优先，保持实验性质。
- 当前范围是 Windows 11 x64、单用户、主屏桌面组件；不要未经确认扩展到多屏、安装器、云同步或商业化发布。
- 仓库名是 `Widgets`，运行时身份仍为 `Widget`。保留 `Widget.exe`、`WIDGET_*` 环境变量、配置文件名和自启动识别规则，避免破坏已有本地配置。
- WorkerW 只用于主屏组件窗口，管理器保持普通窗口。M0 探针是研究入口，不等同于产品功能。

## 工作边界

- 不提交真实配置、便签、凭据、token、环境文件、日志、诊断输出或构建产物。
- 不把示例数据或概念图当作实机证据；不把历史测试结果写成本轮通过。
- 不执行 GitHub 推送、远程发布或可见性变更，除非得到明确授权。
